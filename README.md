# PO Classifier (Streamlit + Groq)

## Setup

1. Add Groq API key to Streamlit secrets:

.streamlit/secrets.toml
```
GROQ_API_KEY = "your_groq_api_key"
```

2. Install dependencies:
```
pip install -r requirements.txt
```

3. Run:
```
streamlit run app.py
```
